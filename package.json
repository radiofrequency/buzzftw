{
  "name": "buzzftw",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build:api": "npm run build --prefix packages/buzz-api",
    "build:runtime": "npm run build --prefix packages/buzz-runtime",
    "sync:directory": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/sync-buzz-directory.ts"
  },
  "dependencies": {
    "astro": "^7.1.4"
  }
}
